Lee `AGENTS.md`, `docs/PROJECT_CONTEXT.md` y `docs/DATA_DICTIONARY.md`.

Inspecciona el repositorio y presenta un plan breve. En esta primera tarea NO entrenes modelos.

1. Valida Python, Java, Spark, PySpark, Hadoop, HDFS y Docker.
2. Crea un método seguro para inspeccionar el `.xlsm` sin cargar toda la hoja innecesariamente.
3. Documenta la exportación a TSV/CSV preservando fechas.
4. Implementa `src/01_ingest_profile.py` para leer desde HDFS con esquema explícito, contar registros/columnas/particiones, calcular nulos, duplicados y cardinalidad, y guardar el profiling.
5. Implementa `src/02_clean_to_parquet.py` para convertir tipos, fechas, crear fecha_corte, credito_cerrado y variables derivadas, sin definir aún el target final, y escribir Parquet trusted.
6. Documenta comandos HDFS y `spark-submit`.
7. Crea tests con datos sintéticos anonimizados.
8. No imprimas PII.
9. No inventes códigos de riesgo.
10. Ejecuta lo que sea posible y reporta qué no pudo validarse.
